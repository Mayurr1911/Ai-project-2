# Real Estate Price Prediction

This project uses Linear Regression to predict house prices based on features like size, location, and amenities.

## Requirements
- Python 3.x
- Install dependencies: `pip install -r requirements.txt`

## Usage
Run the model.py script to train and save the model.

## Dataset
Uses synthetic real estate data for demonstration.
